pipeline{
    agent{ docker { image 'node:14'
            args '-u root:root'}}
    
    stages{
        stage('Dependency install'){
            
            steps{
                sh 'npm install'
            }
        }
        stage('package'){
            steps{
                sh 'npm pack'
            }
        }
    }
}